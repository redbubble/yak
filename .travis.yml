language: go
go:
  - 1.x
install:
  - GO111MODULE=off go get gotest.tools/gotestsum
jobs:
  include:
    - name: check formatting
      script:
        - make fmt && git diff --exit-code
    - name: run tests
      script:
        - make test
